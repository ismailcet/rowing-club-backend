spring:
  application:
    name: app

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  datasource:
    url: jdbc:postgresql://localhost:5432/akk
    username: postgres
    password: Ic110711.

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  threads:
    virtual:
      enabled: true
app:
  file:
    upload-dir: uploads/receipts
  jwt:
    secret: pg24hcoOe3udzEx7JRYHW2hCGrc97fD1HTHBQWbDzpMqZOQ+4cZUo0MFJLPPxXi8aOFRpkKQIhxuPYmGzQuvBg==
    access-expiration: 86400000
    refresh-expiration: 604800000
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    url: /v3/api-docs
  api-docs:
    path: /v3/api-docs

server:
  port: 8080
  forward-headers-strategy: framework